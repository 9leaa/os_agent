# 当前开发进度

更新：2026-09-21。主路线：[Pi 通用 Agent → Computer Use → 后续 OS 能力](Pi_Agent_Development_Plan.md)。

## 已有实现与未完成项

| 项目 | 状态与证据范围 |
|---|---|
| VM / Driver 环境（历史 M0） | 历史真实验收通过：截图、应用信息、普通账户、隔离与基线恢复 |
| 固定计算器流程（历史 M1） | 历史真实完成 12×34，新 AX 显示 408；17 次 Driver 调用，独立 smoke 断言通过 |
| Python 实现 | tools/mac_vm/ 内保留 preflight.py、driver_smoke.py 和 7 项边界测试 |
| Lume 隔离 | patches/cua/ 保存 5 文件补丁，含 2 项隔离测试；历史构建与 VM 隔离验收通过 |
| Pi / Node.js / 模型 | Pi 未安装、版本未锁定、鉴权和图片／工具链路未验收 |
| A0–A1 / C0–C3 | 待开始；没有 Pi 驱动桌面的通过记录 |
| 旧模型路线 | 已停止，旧 M2 未通过，M3/M4 未完成；未实现 result.txt 完整验证闭环 |

历史恢复副本有 Python 3.12.14 与开发凭证保存记录，不能据此宣称模型可用。最后的 VM 运行记录为停机；本次未启动或检查实时状态。

## 本轮仓库清理

按用户要求只保留当前实现和当前 Pi 开发文档：
- 移除整套 Cua 上游源码副本、四份旧方案／交接文档及重复证据说明。
- 三个 Python 实现／测试文件迁到 tools/mac_vm/，迁移前后逐字节一致。
- 提取 Lume 五文件隔离补丁，保留来源、固定基点、MIT 许可证与重建说明。
- 精简 README、进度、环境清单，修复当前计划与协作文件中的路径和引用。
- 旧文档、完整历史日志和上游副本仍在 Git 历史；未重写历史，不删除宿主原开发目录、VM、镜像或私有证据。

验证：迁移后 7 项 Python mock 单元测试通过；补丁在固定上游基点的临时索引前向检查、当前本地实现反向检查均通过；8 份 Markdown 相对文件链接和环境 JSON 检查通过。补丁文件中的空白上下文行需保留前导空格，使用仅针对补丁的 Git 属性避免误报；最终 git diff --cached --check 通过。没有重跑 Swift 构建、真实 VM 或模型测试。

## 下一步

从 A0 开始固定 Pi/Node.js/模型，提供隔离运行配置与启动入口，完成通用文件任务及图片链路预检。不要继续旧 Claude SDK 安装路线。

后续每次更新记录：改动、固定版本、测试命令、测试类型（mock／模型／真实 VM）、失败与阻塞、下一步。历史通过不等于当前重测通过。
