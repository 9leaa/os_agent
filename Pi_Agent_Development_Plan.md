# Pi 通用 Agent 与 Computer Use 开发计划

版本：v0.3 · 日期：2026-09-21 · 状态：路线已确定，A0–C3 均待实施。

## 1. 目标与选型

先交付一个能直接使用的通用 Agent，再在其上开发更强的 Computer Use。macOS 原生接口库属于后续 O0，不作为当前交付门槛。现有 macOS VM 只是第一套测试环境，不承诺已支持其他操作系统。

主基座固定选择 **Pi**，不继续并行选型。复用其现成 TUI（终端聊天界面）、会话、上下文和 Agent 循环，通过扩展与 SDK 增加项目能力；不从裸循环自建通用平台。Pi 接入拟用 TypeScript，既有 Python 工具保留。

当前未固定 Pi/Node.js 版本，未安装 Pi，未做模型与 Computer Use 集成。A0 负责核实版本、许可证、工具 API、依赖和模型兼容性。历史模型选择为 DeepSeek，优先验证；若文本、图片或工具协议不满足要求，报告证据并确认替代模型，不默默切换或读取个人凭证。

官方依据（2026-09-21 核对）：[Pi 功能与扩展](https://github.com/earendil-works/pi/blob/main/packages/coding-agent/README.md)、[SDK](https://github.com/earendil-works/pi/blob/main/packages/coding-agent/docs/sdk.md)。官方支持不等于本项目已验收。Pi 的会话续接不等于电脑现场恢复，TUI 不等于桌面 GUI。

## 2. 架构与范围

专业定位：单 Agent 运行时，加受控工具适配与独立验证。直白说：复用 Pi 负责连续干活，我们开发它安全操作电脑的能力。

- 主循环、模型调用、会话与上下文：复用 Pi。
- 通用工具：任务目录文件读取、生成产物，按用例补充检索或受限数据处理。
- Computer Use：项目扩展调用受控执行适配器，再连接 Cua Driver；截图以模型可读取的图片内容返回，不仅传一个模型不可访问的本地路径。
- 权限、目标身份、调用预算、停止派发、审计：执行端落实，不能仅靠 Prompt 或工具描述。
- 完成判定：任务结束申请交给独立验证器；开放式任务缺乏充分证据时明确标为未验证。
- Agent、工具执行和任务数据先留在现有测试 VM；宿主只做开发、VM 管理和人工查看。不因换 Pi 而扩大宿主权限。

Pi 核心不直接提供 MCP 集成；初期优先实现受控自定义工具适配，不为接 Cua 开放任意 Bash。若后来采用 MCP 扩展，单独评审来源、固定版本并验证图片与权限链路。参见 [Cua 接入说明](https://cua.ai/docs/how-to-guides/driver/connect-your-agent)。

不提前加入 Agent-S、Jev、第二套主循环、复杂路由、多 Agent、长期记忆平台、独立桌面 App。后续只有明确用例或失败记录支持时才引入。

## 3. 阶段总览

| 阶段 | 用户可见交付 | 通过后才能进入 |
|---|---|---|
| A0：可用 Pi | 可在终端使用的通用 Agent | A1 |
| A1：二开基线 | 新工具接入、记录、停止、审批与会话续接 | C0 |
| C0：最小 Computer Use | 观察真实界面并完成简单任务 | C1 |
| C1：增强 Computer Use | 较长流程、跨应用和常见界面变化 | C2 |
| C2：可靠性与评测 | 故障恢复、人工接管和可重复结果 | C3 |
| C3：开发底座发布 | 第二位开发者能部署并扩展 | 后续业务／O0 |
| O0：OS 原生扩展 | 另行确定的系统／应用接口能力 | 不在本轮排期 |

安全限制从 A0 起生效；C2 是集中故障验收，不是到 C2 才加入安全。两人可并行开发 mock 和执行适配，但前序未通过不能宣布后序验收完成。

## 4. A0：先交付能使用的 Pi

目标：用户能直接在隔离测试环境里使用 Pi，而不是只看到框架设计。

工作：
- 在本地项目目录核对并固定 Pi 包、源码版本、Node.js、模型标识及配置；使用项目专用配置和开发凭证。A0 不启动 VM，也不操作宿主桌面或个人文件。
- 复用官方终端交互入口，提供项目启动说明；限制自动加载的扩展、Skills、上级配置和工具。
- 默认仅开放经审查的任务目录读写。Pi 默认工具不是沙箱；必须禁用或替换可越界的默认工具，不把关闭 Bash 当作全部边界已完成。
- 首版不开放任意 Shell。CSV 等数据任务先用受控函数；代码沙箱有具体需求时再单独定义文件、进程、网络、时间边界。
- 提供基础停止入口、运行记录与产物读回；不要求先开发完整恢复系统或 GUI。

固定验收：
1. 连续三轮对话，能按用户新增条件修改新产物。
2. 读取三份测试文本，提取规定字段并生成 Markdown。
3. 读取测试 CSV，用受控统计工具输出 JSON 和报告，独立核对统计值。
4. 取消任务后不派发新动作；越界读取／写入被拒绝。
5. 截图输入和自定义工具图片输出能被所选模型实际读取，为后续 Computer Use 做兼容性预检，不执行桌面动作。

每例至少真实通过一次，保留失败记录；安全用例必须全部通过。交付锁定依赖、无密钥配置示例、启动步骤、验收摘要。未满足时记录阻塞，不重开无边界的框架比较。

## 5. A1：建立可二开的通用 Agent 基线

目标：在不修改 Pi 核心循环的前提下接入新能力，并保留 A0 的可用性。

工作：
- 提供一个自定义工具和一个 Skill 示例，明确 Skill 是流程说明、不授予权限。
- 建立最小事件格式：session_id、run_id、call_id、工具、脱敏参数、结果、耗时、产物引用和验证结论。
- 在执行入口提供批准／拒绝、停止与调用预算；拒绝后不能换另一工具绕过。
- 复用 Pi 会话与上下文压缩，验证重启续接和压缩后任务约束仍存在；任务边界由程序保存，不依赖摘要。
- 会话恢复后暂停待确认；此阶段不承诺自动恢复外部副作用。

验收：另一位开发者按示例新增一个工具，不修改主循环；批准前无动作、拒绝后无副作用；停止后无新派发；重启后能找回对话和任务记录；不同会话不串数据；A0 回归全部通过。

## 6. C0：最小 Computer Use

目标：Pi 根据真实观察操作测试电脑；已有固定脚本只用于环境诊断，不能冒充模型执行。

工作：
- 复核现有 VM 与 Driver，保留历史 M0/M1；接入观察、点击、输入、滚动等按用例授权的工具。
- 每次操作绑定新观察及允许的应用／窗口；坐标、元素和目标失效时重新观察，不盲点。
- 优先完成计算器回归，再在本地测试网页或测试文档中完成输入与滚动任务。
- 保存动作轨迹、操作后状态、产物和独立验证结论。计算器期望值只供验证器，不反馈为模型答案。

验收：旧计划九组加减乘均完成真实操作、结果保存与独立核验；另完成三个固定界面用例（表单输入、滚动定位、测试文档保存），每例至少通过一次。越权目标、陈旧观察、结果缺失、动作超时和调用超限用例必须符合安全规则。原 M2–M4 的有效验收要求纳入此阶段，但不恢复旧 Claude SDK 路线。

九组固定回归：27+16=43、8+9=17、125+75=200；90−17=73、50−28=22、101−1=100；12×34=408、7×8=56、15×6=90。每次新建任务目录，检查实际输入／操作轨迹、新界面显示、result.txt 读回和独立期望值；缺证据不可通过。当前已实现的固定 smoke 仅覆盖 12×34 且没有 result.txt，其他用例仍待实现。

## 7. C1：增强 Computer Use

目标：不只会做单步演示，能完成较长、跨应用的真实操作流程。

工作：
- 预先固定 6 个代表性用例，覆盖跨应用传递、弹窗、窗口变化、输入纠错、较长流程和失败后的重新观察。
- 保存必要截图及其版本／时效；管理图片历史，不把旧图摘要当作当前界面。视觉定位与 AX 信息按可用性使用，不把 AX 封装扩张成完整原生 OS 工具库。
- 对 C0 的失败分类：观察不足、目标定位错误、决策错误、动作未生效、验证不足；逐类改进。
- 只有明确视觉或规划短板时才对照 Agent-S；只有实际成本／延迟瓶颈时才评估 Jev。不将它们列为阶段依赖。
- 首版继续使用 TUI 与独立 VM 查看入口。若需要集成聊天和实时桌面，另立 UI Issue，不阻塞能力测试。

验收：固定同一模型、环境、用例和预算，改动前后各运行每例三次，保留全部记录；全部 6 类用例至少成功一次，并报告成功率、人工介入、工具数和耗时。不得仅展示最佳录像；无可重复收益的新增组件不并入默认路线。

## 8. C2：可靠性与评测

目标：任务可控、失败可解释、恢复不重复副作用。

工作：
- 实现人工接管：先停新派发，记录在途动作，确认控制权后交给用户；继续前重新观察。
- 测试动作已生效但响应超时、进程退出、窗口关闭、用户改文档、模型失败、权限撤销和预算耗尽。
- 恢复同一任务保持原预算，核对副作用与当前状态；不把会话历史当作桌面快照或自动幂等。
- 独立验证器负责结果；模型不能修改审计日志、原始截图或验证报告。结果未知不能标成功。
- 新模型／依赖升级单独跑兼容回归，记录实际版本和可获得的用量，不猜费用。

发布门槛：沿用 C1 六例，每例预先固定三次，共 18 次，至少 17 次独立验证通过且每例至少成功一次；原计算器九例和 A0/A1 关键回归通过。安全、取消、恢复及虚假成功测试必须全部通过；任何越权或误报成功阻止发布。18 次只是项目验收线，不代表通用 benchmark 成绩。

## 9. C3：发布可扩展底座

目标：其他开发者不依赖作者现场帮助，也能使用和扩展。

交付：
- 固定版本、可重复启动步骤、配置模板、工具／Skill／验证器示例和已知限制。
- 从干净代码副本与停机 VM 基线恢复，重新注入个人开发凭证。
- 第二位开发者独立接入一个新任务，完成正常与失败用例；不改 Pi 核心循环。
- 汇总 C2 完整报告并发布建议版本 `pi-computer-use-v0.1`（当前尚未创建）。
- 有第二台机器才登记跨机器复现；同机独立 VM 克隆不得写成跨机器验证。

O0 不随此发布自动启动。后续按业务需要决定是否加入 macOS 原生系统／应用接口、其他 OS 后端或更多交互界面。

## 10. 通用执行边界

- 受测 Agent、文件与证据留在测试 VM；不共享宿主工作目录／剪贴板，不默认关闭 SIP，不授予 sudo 或全盘访问。
- 任务声明允许应用、路径、URL、工具和预算。新能力由对应实现 PR 明确扩范围，不能因本计划提及就默认授权。
- 默认每任务 30 次实际工具执行请求；观察、失败尝试、重试和封装内部请求均计数。包装入口不重复计数，内部实际请求逐项计数；记录拒绝事件但不把未派发动作写成已执行。
- 新鲜验证观察也占预算，预留后再执行；耗尽后仅离线检查已有证据。恢复、接管或会话压缩不能清零计数。复杂用例若需更高上限，先在测试协议和策略中明确变更，不能运行中临时放宽。
- 动作超时先记 UNKNOWN；预算内重新观察，不能盲目重放。call_id 只用于追踪，不提供幂等性。
- 初期沿用 RUNNING、SUCCEEDED、FAILED、BLOCKED、UNVERIFIED 结果语义；停止／审批等控制事件单独记录，结束不等于成功。
- 不登录个人账号、不发送邮件或购买、不开放任意命令执行。模型凭证只由运行时持有，不放进模型可读任务目录。
- 发布前检查秘密、个人信息和原始截图；不默认上传完整会话。

## 11. 两人协作与首批 Issues

| 分工 | 责任 |
|---|---|
| A：Pi 与交互 | 版本锁定、模型、TUI、扩展、会话、控制事件 |
| B：执行与验证 | 工具边界、文件与测试数据、Cua 适配、VM、独立验证 |
| 共同 | 接口约定、PR 互审、真实验收、交叉复现 |

两条工作线先约定工具请求／结果／事件，再各自用 mock 开发。真实桌面同一时刻只能一个人或 Agent 操作；具体见 [协作说明](COLLABORATION.md)。

首批任务（仅清单，尚未创建 GitHub Issues）：
- PI-01：固定 Pi／Node.js／模型与隔离配置，提供项目启动入口。
- PI-02：受控文件工具、测试输入与 A0 独立验证。
- PI-03：图片输入／工具图片输出兼容性探针。
- PI-04：扩展示例、审批／停止／预算、会话与压缩回归。
- CU-01：复核历史 VM 与 Cua，建立 Pi 到执行器的受控接入。
- CU-02：计算器九例及三个基础界面任务。
- CU-03：固定六类增强任务、记录 C0 基线后逐项改善。
- CU-04：恢复／接管／安全评测与第二人部署。

每张 Issue 写清负责人、依赖、交付、测试命令、通过条件和失败证据。A0 不等待 Computer Use 开发完成；但 C0 验收必须在 A1 基线通过之后。

## 12. 已有资产与实施状态

当前只保留 tools/mac_vm/ 的实现与测试、patches/cua/ 的 Lume 隔离改动和必要环境清单；上游源码、旧交接与 v0.1/v0.2 方案已从当前树删除，Git 历史可恢复。九组计算器要求已迁入本计划，无需再读取旧计划才能开发。M0/M1 是历史资产，不等于 A0/C0 已通过；旧 M2–M4 未完成的事实不改写为成功。

本轮仅整理文档和现有源码布局，不安装依赖、不启动 VM、不调用模型、不创建 Pi 实现或发布标签。实际进度以 [PROGRESS.md](PROGRESS.md) 为准。
