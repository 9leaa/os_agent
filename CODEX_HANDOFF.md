# Codex 开发交接说明

> 历史参考（2026-09-21 更新）：当前主路线已改为 **Pi 通用 Agent → Computer Use → 后续 OS 原生能力**。请先读 [Pi 开发计划](Pi_Agent_Development_Plan.md) 和 [当前进度](PROGRESS.md)。下文保留原始设计／交接语境；旧 SDK 选型、阶段顺序和启动指令不再是当前开发指令。计算器与隔离验收要求继续作为回归参考。

日期：2026-09-20。交接状态：文档已准备；尚未投递到 Codex 会话，尚未完成任何目标机器开发验收。

## 一、用户要做什么

用户希望做一个 macOS 上的 OS Agent，关注的不只是看截图点鼠标，也包括将来使用应用接口、系统能力和可验证的执行流程。用户明确提出，早期不应直接操作日常使用的真实 Mac，而应在虚拟机或合适的隔离环境中开发和测试。

讨论已经收敛：底座用 Cua，借鉴 Microsoft UFO 的单设备架构；先做最小 MVP，不从零造虚拟机、Driver、Agent 循环，也不把 Windows UFO 整套移植到 Mac。

用户特别指出：`filesystem.delete()` 与 CLI 删除在能力上大量重叠。因此文件工具应围绕结构化、权限与验证来设计，不能仅靠多封装几个工具就宣称完成了 OS Agent。

本轮不是继续比较选型，而是进入实际开发。首个可交付任务：VM 计算器运算 → 获取实际界面结果 → 保存 → 独立核验。

## 二、交接包内容和使用方式

| 文件 | 用途 |
|---|---|
| AGENTS.md | 开发中必须保留的范围、约束和进度记录规则 |
| Mac_OS_Agent_MVP_Plan_v0.1.md | 原完整计划书，含每阶段 UFO 源码、类/函数、验收条件和参考链接 |
| CODEX_HANDOFF.md | 本文件：用户意图、工作区接管和首轮任务 |
| PROGRESS.md | 当前真实进度及后续更新模板 |

请完整阅读，不假定能访问此前 ChatGPT 对话。原计划书在包内按原内容复制，没有宣称完成的新增实现。

用户操作：把本包解压到 Mac 上一个独立项目目录，在本地 Codex 工作区打开这个目录，然后发送本文件第六节的启动指令。使用现有 Cua 工作区时，先将材料保存在单独子目录，再由 Codex 合并规则，不能覆盖上游 AGENTS.md。

官方 AGENTS.md 说明：
https://developers.openai.com/codex/guides/agents-md

Codex 按工作目录范围读取项目指令，因此移动文档、进入嵌套 Git 仓库或换会话后，要重新确认实际生效的规则和文档路径；不能假定仓库外的交接文件会自动加载。接管后可以把文档放进 Cua 仓库的 `docs/mac_agent_mvp/`，并在本项目作用域内添加规则与指向文档的路径，保留上游规则。

## 三、先确认当前工作区，不要误操作

1. 检查工作目录和 Git 状态，确认不是随心记、无限思维导图或其他无关项目。不要改其他仓库、远程服务器基础设施或既有个人配置。
2. 如果目录里只有交接材料，可将 Cua 上游 clone 到独立 `cua/` 子目录，先做本地开发；已经有 Cua/Fork 就沿用且保留未提交改动。远程 Fork、公开、推送等外部写入并未在本交接中执行或默认授权。
3. 源码根目录下的实现路径为 `samples/mac_agent_mvp/`；交接根目录下有 `cua/` 时，完整路径是 `cua/samples/mac_agent_mvp/`。不要因目录关系误建第二份项目。
4. 读取 Cua 的 README、Agent 示例及必要关联文件，记录实际 commit。计划中的 main 链接可能变化，必须核实具体版本。
5. UFO 只作为源码参考。核实计划固定 commit 可读取；不可用则明确记录，查找并注明替代版本，不能假装已阅读。

## 四、当前首个开发关卡：M0

先完成可安全执行的实际工作，不要只重写一份计划。

**环境检查：** 确认当前是 macOS 宿主、macOS 来宾还是其他运行环境；检查芯片、系统版本、内存、空闲磁盘、Git、Python、Lume/Driver 是否存在。不推断用户一定有 M 系列或足够内存，不在报告中输出设备序列号、凭据等无关敏感信息。

**版本核对：** 根据选定版本官方文档检查本地 macOS VM 路线、Driver 所需系统与权限。以真实源码和帮助信息为准，不直接执行未经核对的旧安装命令、镜像名或参数。

**M0 的 UFO 阅读：**
`documents/docs/infrastructure/agents/overview.md` 的 Three-Layer Architecture。
只用来划清状态、处理流程和执行职责，不搬完整类体系，更不把这些类当成 VM 隔离。

**应有产物：** 在项目 README 中写入环境检查、实际版本、运行位置、隔离配置和后续操作。需要脚本时仅写最小、只读环境检查脚本。准备 VM、安装依赖或请求权限时遵循本地授权；需要人工确认的步骤明确列出，禁止绕过或假报已完成。

**M0 验收：** 真正在目标 macOS VM 中取得有效截图和应用信息，确认没有共享宿主目录和剪贴板，记录版本及干净基线恢复方式。只完成文档、Linux mock 测试或检查脚本不算 M0 通过。

**环境不支持时：** 如当前只有 Linux、没有可访问的目标 Mac、缺权限或目标是 Intel Mac，应准确记录阻塞。继续做能完成的源码核对、文档和只读检查准备，但不要将它们标记为目标 VM 验收，也不要擅自改成操作宿主 Mac。

M0 实际通过后再进入 M1；按关卡继续推进，不以过早增加后续功能掩盖环境问题。

## 五、后续阶段必须保留的 UFO 对照

以下均为项目既定参考，实际开发时先读取源码，再记录适用与不适用的地方；详细链接见计划书。

| 阶段 | UFO 参考 | 借鉴内容 | 本项目最小实现 |
|---|---|---|---|
| M0 | infrastructure/agents/overview.md | State / Strategy / Command 职责分离 | 环境与职责说明；不引入框架 |
| M1 | ufo/module/dispatcher.py；BasicCommandDispatcher、LocalCommandDispatcher | 调用标识、执行结果、超时和异常 | 无模型固定操作及轻量调用记录；超时结果未知不能盲重放 |
| M2 | ufo/agents/processors/app_agent_processor.py；_setup_strategies() | 观察、模型决策、执行、更新记录 | 复用 SDK 循环，工具限权，真实调用计数 |
| M3 | ufo/agents/agent/evaluation_agent.py；AppAgentLoggingMiddleware | 执行与评估分离、基于证据记录 | workspace.py + verify.py；程序核验，不再引入评估模型 |
| M4 | ufo/agents/states/app_agent_state.py；dispatcher.py | 明确终态、失败和确认分支 | 简化状态与异常测试；FINISH 不等于成功 |

计划期望正常加、减、乘共九组和异常用例均按规则通过；目前没有实际通过率或耗时数据。

未来的原生系统工具、CLI/API/GUI 路由是方向背景，不是本版交付范围。不要擅自追加。

## 六、可直接发送给 Codex 的启动指令

```text
请接手这个 Mac OS Agent MVP 项目并开始实际开发，不要再做选型讨论。

先完整读取当前目录的 AGENTS.md、CODEX_HANDOFF.md、
Mac_OS_Agent_MVP_Plan_v0.1.md、PROGRESS.md，再检查实际工作区。

底座已确定为 Cua；UFO 只参考单设备架构，不作为运行依赖。
目标是在 macOS VM 里实际操作计算器，保存界面结果并独立验证。

从 M0 开始：核对本机与虚拟机环境、读取真实上游代码、确认版本，
落实必要的项目初始化和只读检查。每阶段阅读计划对应的 UFO
源码，并将借鉴内容、实际改动和验收证据写入 README/PROGRESS。
上一阶段真实验收通过后再进入下一阶段；不要一次扩成完整平台。

禁止为了跑通去控制宿主 Mac 或访问个人文件、账号；不绕过授权。
Codex 的项目开发命令与受测 Agent 的工具权限要分开。

遇到缺少目标 macOS VM、权限或依赖时，先完成可执行的安全工作，
准确记录阻塞和需要我操作的步骤，不伪造通过、截图或运行结果。
汇报本次改动文件、实际测试、阻塞和下一步，并更新 PROGRESS.md。
```

## 七、本次交接没有做什么

没有直接创建或启动 Codex 会话，没有在用户 GitHub 创建 Fork/分支/提交，没有向用户 Mac 写文件或安装软件，没有启动 VM，没有运行 Agent 或通过测试。本包不含上游源码、模型凭据、可执行安装器或自动化脚本；实际开发由接手的 Codex 按授权完成。
