# AGENTS.md — Mac OS Agent MVP

## 先读与实际状态

先完整读取同目录的 `CODEX_HANDOFF.md`、`Mac_OS_Agent_MVP_Plan_v0.1.md` 和 `PROGRESS.md`，再检查实际工作区。文件迁入仓库后，更新这些相对路径。

这是一个新项目的交接材料，不是已实现的软件。交接时仅准备了文档；没有创建 Codex 任务、没有访问用户 Mac、没有创建远程 Fork、没有部署虚拟机，也没有执行 M0–M4 验收。以磁盘、Git 状态和真实运行结果为准。

## 目标和选型

- 长期方向：安全、隔离、可验证的 macOS OS Agent；当前只做最小桌面任务闭环。
- MVP：在 macOS VM 内实际操作计算器完成正整数加、减、乘，读取计算器显示，保存本次任务的 result.txt，再独立验证。示例：12 × 34。
- 二开底座：`https://github.com/trycua/cua`。计划入口：`libs/cua-driver/examples/agent-sdks/claude_agent.py`。先读取该文件及关联实现，核实固定版本，路径变化时定位实际替代项，不编造接口。
- 参考：`https://github.com/microsoft/UFO`，计划参考 commit 为 `be75a7ded2ad98d97819e15ff1b39d4202ac3ac5`。只参考单设备 AppAgent、Dispatcher、评估与状态处理；不是运行依赖。
- 默认复用 Python、官方示例的 Agent SDK 和 Cua Driver 接入。不要因为开发工具是 Codex，就擅自把应用运行时的模型或 SDK 换成 Codex。
- 在 Cua 根目录下新增 `samples/mac_agent_mvp/`，尽量不修改 Lume、Driver 的底层代码。

## 工作边界

不加多 Agent、Galaxy、多设备调度、长期记忆、训练、通用 Action Router、数据库、Web 前端、邮件、个人账号登录或完整原生工具库。封装文件函数的价值是受控接口，不把它描述成 CLI 无法提供的新 OS 能力。

按 M0 → M1 → M2 → M3 → M4 推进。每阶段先读计划指定的 UFO 文件和类/函数，再进行对应实现；未实际验收不得标记完成。不得把上游注释、Prompt 约束或一个返回状态当成已经具备的安全保证。

## 运行与安全

受测 Agent 的桌面操作、文件写入和任务证据必须位于 VM。不能为跑通示例而临时操作宿主计算器、Finder、个人文件或真实账号。

开发工具 Codex 可以在授权项目目录内使用 Git、编辑器和测试命令；这是开发权限，不代表受测 Agent 可以获得任意 Shell。宿主只执行必要的项目开发、只读环境检查和 VM 管理。

不共享宿主工作目录或剪贴板；不默认关闭 SIP；不授予免密 sudo 或全盘访问；不读取用户现有凭据来充当开发凭据。依赖安装、虚拟机镜像下载、系统权限等需要额外授权时保留确认，明确说明作用域；不得绕过。

工具白名单、计算器窗口身份、参数和任务目录边界必须在执行端检查。Prompt 不是权限边界。禁止任意 Shell、其他应用和外部写操作；规则拒绝后不得经 GUI 或另一工具绕过。

实际工具调用总量上限为 30，需自行准确计数；SDK 的 turn 数不等于工具调用数。达到上限不得继续发起 Driver 观察或其他工具请求；证据不足则返回未验证或失败。

带副作用的动作超时代表结果可能未知，先观察后决定，不能盲目重复。观察仍受同一权限和调用预算约束。call_id 用于追踪，不自动提供幂等性。

## 验证与状态

不能让模型自己算出答案并直接写文件来冒充操作完成。期望值只供独立校验，不得反馈为执行答案。禁止 eval 用户表达式。

校验应同时核对实际运算轨迹、操作后新的界面显示、读回文件和独立期望值。优先使用可靠辅助功能数据；读不到就记录 UNVERIFIED，不用模型猜。模型不得修改日志、截图或验证报告。

自定义任务状态：RUNNING、SUCCEEDED、FAILED、BLOCKED、UNVERIFIED。结束不等于成功；UFO 的 FINISH 不可直接映射为成功。只有独立验证通过才为 SUCCEEDED。

## 版本、改动和交接

保留上游许可证、来源与已有 AGENTS.md；不覆盖用户改动，不修改无关项目，不自行创建远程仓库或推送。没有 Fork 时先在独立子目录 clone 上游并使用本地开发分支，不把非空交接目录当作空仓库覆盖。

每阶段更新 PROGRESS.md 和项目 README：读过的 UFO 源码及 commit、借鉴和未采用内容、实际修改、测试命令、证据、阻塞、下一步。区分未运行、mock/unit 测试和真实 VM 集成验收；环境不足不能伪报通过。

用中文汇报，先说明实际完成什么、改了哪些文件、测试结果和阻塞。不要重复已经确定的选型；不要擅自扩大 MVP 范围。
