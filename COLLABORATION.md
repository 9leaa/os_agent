# 多人开发与 VM 调试

当前路线见 [Pi 开发计划](Pi_Agent_Development_Plan.md)。仓库只保存代码、规则、版本声明和脱敏验收摘要，不保存凭证、VM 磁盘或原始桌面数据。

## 分工与 PR

- A 负责 Pi 接入、TUI、模型、会话和控制事件；B 负责工具约束、Cua、VM 和验证；共同评审和验收。
- 从 main 建短分支（默认 codex/ 前缀），每个 PR 只交付一个可验收目标。Pi 代码拟在根 agent/，已有诊断保留 cua/samples/mac_agent_mvp/。
- 先约定请求／结果／事件格式，再用 mock 分别开发。无需桌面的测试可在各自机器运行。
- PR 写明阶段、实际测试、未测项和风险。更新 PROGRESS，不把单元测试当作模型或桌面验收。
- 上游 Pi/Cua 升级单独 PR，不与业务功能混合。保留来源、许可证及固定版本。

## 真实 VM 调试

1. 从停机、无模型凭证的基线创建独立克隆，不覆盖现有副本，不共享正在写入的磁盘。
2. 维护者先检查待测 PR 代码，再将明确 commit 部署到 VM，注入开发专用凭证。
3. 记录开发者、VM 名、commit、Pi/Node/Driver/模型版本和使用时间；只有一台测试 Mac 时预约串行调试。
4. 同一桌面同一时刻只允许一个人或一个 Agent 控制。人工接管先停止新动作，继续前重新观察。
5. 原始证据留在受控存储；GitHub 仅上传逐项检查后的摘要。公共仓库 Artifact 不默认私密。
6. 测试结束正常停机，按约定保留诊断数据。删除临时 VM 必须确认具体对象与授权，不把清理当作隐含授权。

现有 macOS 后端需要 Apple Silicon 测试机。没有该设备的开发者先做 mock 和协议测试，再预约测试机；不宣称其他 OS 已适配。16 GiB 宿主先按单 VM 串行安排，不能默认同时跑多个 8 GiB VM。

远程人工查看只通过受控连接进入测试机／VM，不把 VNC 或管理端口直接暴露公网，不共享宿主工作目录或剪贴板。

## 凭证与 CI

公共 CI 仅跑无凭证检查；不让外部 PR 自动访问带凭证的自托管测试机。SDK 安装脚本、Pi 扩展和依赖也要审查，不能把扩展视为沙箱。

模型凭证不进入源码、会话、截图或可被模型读取的任务目录；不复制个人已有认证。共享基线必须确认不含凭证，不能直接分发当前开发副本。

## 历史环境

[vm-manifest.json](vm-manifest.json) 只声明已有 macOS 环境，不代表 Pi 已配置或新阶段已通过。现有 configured 基线含系统、测试账户和 Driver，不含后续 Python/Pi/模型凭证；恢复副本曾保存开发凭证，不得当作共享镜像。任何镜像分发需先核验许可、恢复步骤与敏感数据清理。
