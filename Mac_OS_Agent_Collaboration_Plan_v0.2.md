# Mac OS Agent 联合开发计划书 v0.2

> 历史参考（2026-09-21 更新）：当前主路线已改为 **Pi 通用 Agent → Computer Use → 后续 OS 原生能力**。请先读 [Pi 开发计划](Pi_Agent_Development_Plan.md) 和 [当前进度](PROGRESS.md)。下文保留原始设计／交接语境；旧 SDK 选型、阶段顺序和启动指令不再是当前开发指令。计算器与隔离验收要求继续作为回归参考。

日期：2026-09-21。状态：评审草案，所有新增阶段均未验收。
设计依据：[整体设计方案](Mac_OS_Agent_Design_v0.2.md)。本计划保留 [v0.1 计算器计划](Mac_OS_Agent_MVP_Plan_v0.1.md) 作为基础回归规范。

## 1. 交付目标与现状

联合开发的第一个交付是能接收自然语言并在 VM 内完成真实任务的 Agent；第二个交付是具备常用功能、可被别人复现的 Base Agent；第三个交付才是基于底座的定制业务。

| 当前资产 | 核对结果 | 如何复用 |
|---|---|---|
| macOS VM、普通账户、Driver | 历史真实 M0 已通过 | 恢复并复核现有环境，不从头造 VM |
| 固定计算器流程 | 历史真实 M1 已通过；源码存在 | 作为独立诊断和回归用例 |
| Agent 框架 / 模型 | 旧 Claude SDK 安装未确认；OpenCode 尚未集成 | 对照验证后确定唯一主路线 |
| 结果文件与完整独立校验 | M3–M4 未完成 | 第一版 Agent 的必交内容 |
| GitHub 仓库 | 代码和协作材料已发布 | 短分支、PR、共同评审 |
| 会话、通用工具与交互界面 | 本项目尚未交付 | 在可运行基线之后按用例增加 |

本次只核对文档、源码和远端版本，没有运行 VM 或模型。此前 7 项 Python、2 项 Lume 测试的通过结果是历史记录，不是本次新测。

## 2. 阶段总览

下列工期是假设两名开发者、已有测试 Mac、每天可安排联调的工作日估算。人员投入、模型兼容或机器可用性变化时重新估算；日期不能替代验收。

| 阶段 | 用户可见成果 | 主要工作 | 进入下一阶段的条件 | 估算 |
|---|---|---|---|---|
| J0 框架选型与实测 | 确认用哪个现成 Agent 和模型 | 对照 OpenCode 与旧 SDK 路线，验证图片/工具/权限/恢复 | 选定唯一主路线，固定版本且关键探针全通过 | 2–3 日 |
| J1 首个可用 Agent | 输入任务，真正操作并交付结果 | 复用框架循环及现成入口、执行限制、文件和验证；完成旧 M2→M3→M4 | 计算器 9 组任务与原异常集通过，另一人可执行 | 3–5 日 |
| J2 常用功能 | 能连续对话、读网页、处理文件、人工接管 | 会话/恢复、受控工具、任务进度、现成界面接入、多步骤任务 | 功能用例和故障用例达到下述门槛 | 5–8 日 |
| J3 稳定 Base Agent | 团队可在底座上新增能力 | 扩展契约、模板、干净环境复现、回归与发布 | 第二位开发者完成扩展并独立复现 | 3–5 日 |
| J4 业务定制 | 完成用户指定的真实业务 | 每次增加一个工具/任务包/校验器 | 新业务验收通过，基础回归不退步 | 按具体需求估算 |

J0–J3 合计约 13–21 个工作日，按 3–5 周预留。第一周目标是出现可运行的 J1 演示，不把全部时间投入界面或框架搭建。J4 的具体业务尚未指定，不在此虚构需求或工期。

## 3. J0：固定现成 Agent、模型和环境

负责人 A 主导 Agent 框架/模型，负责人 B 主导 VM/Driver和执行约束，交叉检查结果。

- 优先验证 OpenCode，使用已有会话、TUI/Web 和 MCP；以 Cua 的 claude_agent.py / Claude Agent SDK 路线作为对照。限定 2–3 日完成关键探针和选型报告，不同步搭两套产品。
- 每个候选独立配置、固定依赖；禁止读个人现有登录或默认开放任意 Shell。旧 venv 先检查，未完成环境不登记为安装成功。保留现有 Python 工具和校验代码。
- 同一模型、同一 VM 基线验证六项关卡：文本/图片；工具调用和工具图片返回；多轮续问/重启恢复；取消和停止派发；执行前越权拦截；加入一个项目工具。只使用测试数据与开发凭证。额外核对模式切换、框架内置工具与配置覆盖是否能绕过边界。
- 复核 Driver 权限和应用观察；对比源码快照与安装二进制的真实接口，不把最新网页文档直接当作 0.28.2 的接口。
- 锁定 Agent 框架及其实际运行组件、Python、Driver、Cua、模型标识、VM 基线和策略版本；为可变模型别名记录测试日期与返回的版本信息（若有）。
- 整理 VM 初始化/检查说明，包含构建隔离 Lume 的步骤、来宾权限和人工登录要求，避免依赖某人的私有临时脚本。

交付：框架对照决策记录、最小依赖清单、无密钥配置示例、兼容性报告和环境说明。所有关键关卡通过后，比较新增代码量、现成界面复用和维护成本；OpenCode 优先，旧 SDK 路线是备选。不能限权或工具图片丢失时淘汰该组合。J0 未通过不扩大功能，最终选型在实施 PR 同步更新 AGENTS.md，未经实测不把建议写成已迁移。

## 4. J1：先交付一个真正可用的 Agent

负责人 A 完成框架入口与事件接入；负责人 B 完成受限工具、工作目录和独立校验；两人共同做真实 VM 验收。

实施顺序继承原计划：先 M2 自然语言驱动，再 M3 文件保存/独立验证，最后 M4 异常收口。安全检查从第一次工具接入即启用。

交付内容：

- 一条文档化启动入口，复用框架 TUI/Web 接收中文任务、显示动作进度和最后结果；采用 SDK 备选时提供最小终端入口。
- 框架驱动的计算器操作；输入数字来自任务，不把固定脚本或独立期望值冒充模型执行。
- 只开放该任务需要的工具和窗口；禁用非必要内置工具，不给整个 Driver 无差别授权。
- VM 中每次新建任务目录，保存结果、调用记录、新界面证据和独立验证结论。
- 保留 30 次实际工具调用上限；终止、权限拒绝、超时结果未知均可追踪。
- 基础停止入口。停止框架执行并不代表动作自动回滚，收尾需说明未知状态。

验收：旧计划的加、减、乘九组任务各完成一次且全部通过独立校验；原异常集全部符合预期。禁止把不完整运行记成成功。至少由另一位开发者从文档入口执行一次，不依赖作者临场修改代码。

交付名建议为 agent-preview。这是第一版可用闭环；仍以计算器和受控文件范围为主，常用功能在下一阶段增加。

## 5. J2：补齐常见 Agent 功能

本阶段扩大旧 MVP 的应用与界面范围。相应 PR 先同步 AGENTS.md、允许工具和执行参数边界，再开放新能力；不能只修改 Prompt。

| 功能包 | 交付 | 关键验收 |
|---|---|---|
| 对话与上下文 | session_id、多轮续问、会话列表和恢复 | 三轮追问关联正确；不同会话不串数据；重启可恢复测试会话 |
| 任务执行控制 | 进度事件、取消、暂停、人工接管与继续 | 接管后无新自动动作；继续前重新观察；同一任务不重置预算 |
| 受控文件 | 任务目录内读取、列举和生成文本/JSON | 检查内容、读回与路径；越界、符号链接和意外覆盖被拦截 |
| 浏览器/页面 | VM 内测试页面读取、提取和表单操作 | 独立检查测试服务状态；请求/跳转逐次检查允许的 URL |
| 桌面扩展 | 第二个测试应用，优先 TextEdit | 只处理任务目录中的测试文档；窗口及文件访问边界有效 |
| 任务模板 / 工具接入 | 显式注册一个新工具和一个可复用流程 | 配置不能扩大执行权限；用例可独立验证 |
| 界面复用 | 现成聊天、会话列表、停止/继续，补项目产物和验证状态 | 优先使用上游 UI 和 API；SDK 备选才做轻量界面；无重复提交 |
| 诊断信息 | 工具数、耗时、错误阶段、实际可获得的用量 | 日志脱敏；第三方模型费用无法确认时不填假数 |

浏览器先用受控本地测试页面，通过后才增加选定公开页面的只读访问。不加入账号、发送、购买或任意下载；只限制应用名称不能约束浏览器或文件对话框，URL、路径和具体操作也要在执行端受控。做不到可靠限制的入口先不开放。

建议的六个端到端用例：

1. 三轮对话引用之前的任务和产物，过程中更正一项输入。
2. 读取任务目录中的测试文本，生成结构化摘要并读回验证。
3. 在 TextEdit 查看并修改任务文档，检查最终文件内容。
4. 从本地测试网页提取指定字段，检查结构化结果。
5. 测试网页取数 → 计算器运算 → 保存结果和来源，核对完整轨迹。
6. 用户停止并人工修改界面 → 明确继续 → 基于新状态完成任务。

测试门槛：每例至少成功一次；预先固定每例三次运行，共 18 次，至少 17 次通过。不能丢弃失败后另挑 18 次。所有越权、取消、未知结果和虚假成功测试必须通过；任何越权或误报成功都阻止发布。失败轮次保留，阈值是建议验收线，不是已有成功率。

原计算器九组回归仍全部通过。真实测试固定一台 VM、一个已验证模型组合，记录耗时和调用数，避免把环境混合数据当作模型比较。

## 6. J3：固化团队可扩展的 Base Agent

负责人 A 与 B 交换使用者角色，验证底座不依赖原作者。

- 固定任务输入、事件、产物和验证结果的最小契约，提供一个业务扩展示例。
- 复用选定框架的会话和工具机制；清理为跑 Demo 加的临时路径和配置，不提前搭通用插件平台。
- 第二位开发者只增加任务说明、工具适配和校验器，接入一个新测试任务；确认不需要改 框架主循环。
- 从新的代码副本和干净停机 VM 基线完成恢复、配置、启动与回归；另一台 Mac 最佳，暂无设备则准确记为“第二位开发者在同机独立克隆复现”。
- 完成全部关键回归，登记已知限制、模型兼容矩阵及恢复办法。
- 通过后发布固定源码版本，建议标签 base-agent-v0.1；此标签当前尚未创建。

发布完成的判断：别人可以按文档跑起来，常用功能有测试，新增能力有稳定接入口，失败能够解释。完整原生工具库、复杂调度或多模型自动路由不属于发布门槛。

## 7. J4：在底座上实现用户业务

先选一个真实目标，以“输入 → 允许动作 → 产物 → 验收条件”写成业务任务卡，再决定需要 API、MCP、GUI 还是组合工具。每个任务单独实施、验收，不用模糊的“让 Agent 更智能”作为迭代目标。

优先级依据真实业务价值和失败成本。长期记忆、知识库、计划任务等只有在业务明确依赖时才加入，并另写存储、权限和测试设计。业务修改必须通过 Base Agent 回归，避免新场景破坏已有能力。

## 8. 两人联合开发与 VM 联调

| 工作线 | 主责 | 可独立推进的内容 |
|---|---|---|
| A：Agent 与交互 | 框架、会话、事件、现成界面接入 | 用 mock 工具测试会话与交互 |
| B：执行与验证 | Driver、文件/网页边界、校验、VM | 固定流程、工具契约和异常用例 |
| 共同 | 设计评审、真实 GUI 演示、合并与发布 | 互查 PR，交叉复现结果 |

第一天先约定最小任务输入、工具结果、事件和验证状态，避免两边分别创造不兼容接口。角色是人类开发者的分工，不代表系统引入多 Agent。

代码流程：一个 Issue 对应一个可验收成果；从 main 创建短分支 → 小 PR → 另一人评审 → 必要的真实 VM 验收 → 合并。只更新到团队发布的固定版本；升级上游 Cua/Agent 框架 单独开 PR，不与业务功能混合。

VM 流程：停机基线 → 每人/每次验收创建独立克隆 → 装入明确 commit 的代码 → 注入个人开发凭证 → 运行用例 → 导出脱敏摘要 → 保留必要诊断后停机清理测试副本。基线不含凭证，远程屏幕使用受控连接；同一桌面同一时刻只有一位人或一个 Agent 操作。

一台 16 GiB Mac 先串行调试。预约记录包含使用者、VM 名、commit、时间和结束状态；初期手工预约足够，不先开发调度系统。没有 Mac 的协作者完成无 GUI 测试后，再用专用 Mac 的独立克隆验收。

公共 CI 只跑无密钥检查；真实 VM 作业先人工检查提交再触发。截图和原始日志留在受控存储，GitHub 只放脱敏摘要和必要的审核后证据。详见 [协作约定](COLLABORATION.md)。

## 9. 首批可以直接拆成 Issue 的任务

| 建议编号 | 成果 | 依赖 | 主责 |
|---|---|---|---|
| BASE-01 | 框架对照、模型兼容性报告和依赖锁定 | 无 | A |
| BASE-02 | 可重复的 VM/Driver 环境检查与恢复说明 | 无 | B |
| BASE-03 | 自然语言 Agent 入口、事件与工具限制 | 01、02 | A |
| BASE-04 | 任务目录、文件工具与独立验证器 | 02；与 03 先约定契约 | B |
| BASE-05 | J1 九组真实任务和异常验收报告 | 03、04 | 共同 |
| BASE-06 | 会话、恢复、停止与人工接管 | 05 | A |
| BASE-07 | 文件/网页/第二应用任务与校验 | 05 | B |
| BASE-08 | 界面集成、扩展示例、第二人复现和版本发布 | 06、07 | 共同 |

这些是任务草案，尚未自动创建 GitHub Issues 或分配账号。每张任务卡需写清负责人、前置条件、交付文件、测试类型、通过条件和失败记录。

## 10. 本次交付与生效边界

本次交付两份评审文档以及 README/PROGRESS 导航；不改运行代码、不启动 VM、不安装 Agent 框架或发送模型请求，也不把任何 J 阶段标记通过。

合并计划后，实施 PR 逐阶段同步适用的 AGENTS.md 和实际策略。旧 M0–M4 是第一版安全闭环的验收依据；J0–J4 描述联合开发路线，两种编号不互相冒充完成状态。
